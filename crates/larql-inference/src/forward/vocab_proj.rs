//! Embedding / unembedding primitives — moved to
//! `larql_compute::forward::vocab_proj` (ADR-0022 follow-up).
//! This shim preserves `crate::forward::vocab_proj::*` paths.

pub use larql_compute::forward::vocab_proj::*;
